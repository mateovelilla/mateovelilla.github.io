---
const {frontmatter} = Astro.props;
---
<html>
  <!-- ... -->
  <h1 class="text-red-500">{frontmatter.title}</h1>
  <h2>Post author: {frontmatter.author}</h2>
  <p>{frontmatter.description}</p>
  <slot /> <!-- Markdown content is injected here -->
   <!-- ... -->
</html>